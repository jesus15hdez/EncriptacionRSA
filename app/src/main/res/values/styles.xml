<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@color/navBar</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
    <style name="secondActivity" parent="Theme.AppCompat.DayNight">
        <item name="colorPrimary">@color/material_dynamic_primary40</item>
        <item name="colorPrimaryDark">@color/material_dynamic_primary30</item>
        <item name="colorAccent">@color/material_dynamic_secondary30</item>
        <item name="android:navigationBarColor">@color/material_dynamic_primary30</item>
    </style>
    <style name="drawerView">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
    <style name="cardViews">
        <item name="cardBackgroundColor">@color/cardView</item>
        <item name="android:textColor">@color/texto</item>
    </style>

</resources>
